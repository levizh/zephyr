# Copyright (C) 2022-2024, Xiaohua Semiconductor Co., Ltd.
# SPDX-License-Identifier: Apache-2.0
---
title: HC32 DMA
version: 0.1

description: >
    This is a representation of the HC32 DMA nodes

properties:
    compatible:
      type: string
      category: required
      description: compatible strings
      constraint: "xhsc,hc32-dma"
      generation: define

    reg:
      type: array
      description: mmio register space
      generation: define
      category: required

    clocks:
      type: array
      category: optional
      description: Clock gate information
      generation: define

    label:
      type: string
      category: required
      description: Human readable string describing the device (used by Zephyr for API name)
      generation: define

    interrupts:
      type: array
      category: required
      description: required interrupts
      generation: define

    interrupt-names:
      category: optional
      type: stringlist
      description: names of required interrupts
      generation: define

    dma-channels:
      type: int
      description: Number of DMA channels supported by the controller
      generation: define
      category: required
...
