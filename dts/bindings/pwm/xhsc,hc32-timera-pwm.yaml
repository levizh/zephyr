---
title: HC32 PWM
version: 0.1

description: >
    This binding gives a base representation of the HC32 PWM

inherits:
    !include pwm.yaml

properties:
    compatible:
      constraint: "xhsc,hc32-timera-pwm"

    addr:
      type: int
      description: timera register base address
      generation: define
      category: required

"#cells":
  - channel
# period in terms of nanoseconds
  - period
...
